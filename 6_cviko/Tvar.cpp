#include "Tvar.h"
#include <fstream>
#include <cmath>
#include <iostream>
#include <vector>
#include <cmath>
#include <random>
#include <cstdlib>


double Tvar::obsah() const {
	int iter = 10000;
	double x_mi = x_min();
 	double x_ma = x_max();
	double y_mi = y_min();
	double y_ma = y_max();
	double r;

    // Pomoci Monte Carla

	return 0;
}